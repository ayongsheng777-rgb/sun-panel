# Sun-Panel AI 增强版功能改造需求规格书 V2.0（整合版）

> 本文由两份规格书整合而成，并对照**线上实际实现状态**标注完成度：
> - 《Sun-Panel AI增强版功能改造需求规格书 V1.0》（初版蓝图）
> - 《Sun-Panel AI智能搜索功能增补规格书 V1.0》（AI 搜索细化）
>
> 更新时间：2026-08-17

## 项目信息

| 项 | 值 |
| --- | --- |
| 项目 | Sun-Panel 导航面板（Go 后端 + Vue3 前端） |
| 代码仓库 | `https://github.com/ayongsheng777-rgb/sun-panel` |
| 更新源 | 已从官方 `hslr-s/sun-panel` 改为上述自有仓库 |
| 部署 | fnOS（飞牛 NAS）原生进程，端口 13002 |
| 数据 | SQLite，路径 `/vol1/@appdata/Sun-Panel/database/database.db`（升级不丢数据） |

---

## 一、实现状态总览

| 模块 | 来源 | 状态 |
| --- | --- | --- |
| AI 智能搜索 | 增补规格书 | ✅ 已实现并上线 |
| 弹性多网址迷你快捷入口 | 初版重构 | ✅ 已实现并上线 |
| 更新源切换为自有 GitHub 仓 | 部署需求 | ✅ 已实现 |
| AI 自动添加网址（联网搜官网+分类+图标+审计） | 初版 V1.0 | ✅ 已实现（本地验证编译通过，待同步上线） |
| 智能图标系统（favicon 优先，失败回退文字） | 初版 V1.0 | ✅ 已实现（同上） |
| OTP 双因素认证（强制所有人） | 初版 V1.0 | ✅ 已实现（同上） |
| 登录设备管理 / 信任设备 | 初版 V1.0 | ✅ 已实现（同上） |
| GitHub 智能检索 | 初版 V1.0 | ✅ 已实现（同上） |
| AI 自动化任务队列 + 审计日志 | 初版 V1.0 | 🟡 部分实现（审计日志已落地 `ai_operation_log`，AI 操作为同步执行+前端 loading，未做独立后台 worker） |
| AI 智能管理权限（AI_ADMIN） | 初版 V1.0 | ⏳ 规划中 |

---

## 二、已实现功能（✅ 已上线）

### 2.1 AI 智能搜索

首页顶部搜索框新增 **✨ AI** 模式，用户输入自然语言后，AI 只负责在**数据库已有网址/应用**里做语义理解、匹配和排序，**绝不凭空生成网址**。

**核心安全原则**：AI 只返回 `itemId`，最终 URL 一律由后端回查数据库，杜绝 AI 幻觉、恶意外链、提示注入。

**技术架构（两阶段检索）**：

```
用户输入 → 本地候选召回(Top30) → AI 重排 → 返回 itemId → 数据库回查真实 URL → 图标卡片展示
```

| 能力 | 实现 |
| --- | --- |
| 本地候选召回 | 标题 0.40 / 描述 0.20 / 内网地址 0.15 / URL 0.10 / 地址名 0.15 / 地址URL 0.05 |
| AI 重排 | OpenAI 兼容适配器，强制 JSON 输出，校验 itemId 去重、≤12 条 |
| 降级兜底 | AI 超时/报错/JSON 解析失败 → 自动降级普通搜索（返回 `fallback` 标记） |
| 服务商 | DeepSeek / NVIDIA / 自定义 OpenAI 兼容（适配器模式，易扩展） |
| 模型管理 | 拉取模型列表、测速（真实发请求「Reply with OK」）、手动指定模型 |
| 搜索缓存 | 前端 60 秒缓存 |

**当前线上配置**（已写入数据库 `module_config.aiSearchConfig`）：

| 项 | 值 |
| --- | --- |
| 启用 | ✅ `enabled=true` |
| 默认服务商 | `nvidia` |
| 模型 | `deepseek-ai/deepseek-v4-flash-0731`（已验证 HTTP 200 可用） |
| Endpoint | `https://integrate.api.nvidia.com/v1` |

**新增 API**：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/panel/search` | `{query,mode,limit}` → `{results,provider,model,fallback}` |
| GET/POST | `/panel/ai/config` | 读取 / 保存 AI 配置 |
| GET | `/panel/ai/models?provider=` | 拉取模型列表 |
| POST | `/panel/ai/test` | 模型测速 |

### 2.2 弹性多网址迷你快捷入口

单个图标支持 **1 个默认地址 + N 个可选地址**：

- 迷你快捷按钮（点击不冒泡、可独立打开）
- 地址类型配色（https 绿 / http 橙 / 内网蓝 / 其他灰）
- 地址 >6 个自动折叠为 `+N`
- 右键菜单、拖拽排序、设默认
- 旧数据 `url`/`lanUrl` 自动迁移到 `addresses`（幂等，不丢数据）

数据结构：`item_icons` 表新增 `addresses_json` 文本列，存 `[]ItemAddress`。

### 2.3 更新源切换

- 「关于」页「检查新版本」、页脚、后端启动日志的项目地址，已全部从官方 `hslr-s/sun-panel` 改为 `ayongsheng777-rgb/sun-panel`。
- 线上已验证：旧官方链接清零，新链接生效。

---

## 三、规划中功能（初版 V1.0 蓝图，尚未实现）

这些是初版 V1.0 的完整愿景，目前**未落地**，作为后续迭代方向保留：

| 模块 | 要点 |
| --- | --- |
| AI 智能管理权限 | 新增 `AI_ADMIN` 角色，允许增改网址/分组/图标/排序，**禁止删除** |
| AI 自动添加网址 | 「添加 ChatGPT 官网」→ 关键词分析 → 联网搜索 → 真实性判断 → 取标题/描述 → 分类 → 图标 → 保存 |
| 智能图标系统 | favicon.ico → Google favicon API → 第三方 → AI 识别 → 本地图标库，五级 fallback，失败不阻塞 |
| OTP 双因素认证 | TOTP RFC6238，兼容 Google/Microsoft Authenticator，首次扫码绑定、换绑 |
| 登录设备管理 | 记录设备/浏览器/IP/登录时间/状态 |
| 信任设备 | 「信任此浏览器 30 天」，`trusted_devices` 表 |
| AI 任务队列 | 所有 AI 操作异步，禁止 HTTP 同步等待 |
| GitHub 智能检索 | 搜索 GitHub 仓库 → 分析 → 生成简介 → 添加导航入口 |
| AI 网址智能分类 | 输入 URL 自动判断分类（开发工具/服务器/NAS/金融…） |

---

## 四、验收状态

### 已通过 ✅

- AI 搜索只匹配库内资源，不幻觉网址（只回传 itemId）
- AI 失败自动降级普通搜索，不阻塞主流程
- 多地址图标：favicon/默认地址/快捷地址独立点击
- 旧数据迁移幂等，重启不丢数据
- 更新源指向自有仓库

### 待实现 ⏳

- [ ] AI 自动添加网址 / 智能分类 / GitHub 检索
- [ ] 智能图标五级 fallback
- [ ] OTP 双因素认证 + 设备管理 + 信任设备
- [ ] AI 自动化任务队列

---

## 五、开发原则（沿用初版）

```
可靠性 > 完美性
自动化 > 手工操作
不中断 > 精确等待
用户拥有最终控制权
```

最终目标：把 Sun-Panel 打造成个人服务器、NAS、AI 工具、互联网入口的统一管理中心。
